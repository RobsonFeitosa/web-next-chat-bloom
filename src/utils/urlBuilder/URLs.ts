const AUTH = '/auth' 
const AUTH_LOGIN = '/login' 

const USERS = '/users' 

const ROOMS = '/rooms' 

const CHATS = '/chats' 
const CHATS_ROOM = '/room' 

export const URLs = {
  USERS,
  AUTH,
  AUTH_LOGIN,
  ROOMS,
  CHATS,
  CHATS_ROOM
}
